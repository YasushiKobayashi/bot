machine:
  timezone: Asia/Tokyo
  node:
    version: 6.7.0
  post:
    - npm install -g npm@3

dependencies:
  pre:
    - npm install -g gulp coffee-script
    - npm install
test:
  override:
    - npm test
    - exit 0

machine:
  timezone: Asia/Tokyo
  node:
    version: 6.7.0
  post:
    - npm install -g npm@3

dependencies:
  pre:
    - npm install
test:
  override:
    - npm run lint
    - npm run build
    - exit 0
